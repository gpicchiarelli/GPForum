% layout 'default';
% title t('auth.password_reset_title') . ' - ' . t('app.name');
% my $form_described_by = $ui->{described_by} || q{};

<section aria-labelledby="<%= $ui->{heading_id} %>">
  <h1 id="<%= $ui->{heading_id} %>"><%= t('auth.password_reset_title') %></h1>
  <%= include 'components/error_summary',
    summary_id => 'password-reset-request-error-summary',
    errors => $errors,
    fields => $error_fields %>
  <form method="post" action="<%= url_for('password_reset_request_submit') %>"<%== length $form_described_by ? ' aria-describedby="' . $form_described_by . '"' : q{} %>>
    <%= csrf_field %>
    % for my $field (@{$fields}) {
      <label for="<%= $field->{id} %>"><%= t($field->{label_key}) %></label>
      <input id="<%= $field->{id} %>" name="<%= $field->{name} %>" type="<%= $field->{type} %>" value="<%= $field->{value} %>" autocomplete="<%= $field->{autocomplete} %>"<%== $field->{error_attrs} %>>
      <%= include 'components/field_error', field_id => $field->{error_id}, message => $field->{error} %>
    % }
    <button type="submit"><%= t('auth.password_reset_submit') %></button>
  </form>
</section>
