% my $tone = stash('tone') || 'notice';
% my $role = $tone eq 'error' ? 'alert' : 'status';
% my $message = stash('message') || q{};

<p class="ui-alert ui-alert--<%= $tone %>" role="<%= $role %>"><%= $message %></p>
