# CPAN::Audit exclusions for `script/cpan-audit` (CPANSA advisory ids / CVEs).
#
# Every line here needs a reason, an owner-visible mitigation, and a review
# trigger. Anything without an entry fails the security scan stage
# (prompt/11.txt). Reviewed with the dependency lock: cpanfile.snapshot.
#
# Review trigger for all entries: any Mojolicious major upgrade, or an upstream
# release that publishes a fixed version for these advisories.

# CPANSA-Mojolicious-2024-58134 / CVE-2024-58134
#   "Mojolicious uses a hard coded string, or the application's class name, as
#   HMAC session secret by default."
#   Not applicable: GPForum never relies on the Mojolicious default. The session
#   secret is required configuration (GPForum::Config session_secret), validated
#   non-empty, rotated through previous_session_secrets, and production config
#   load refuses the development default.
#   Upstream publishes no fixed version; the mitigation is configuration.
CPANSA-Mojolicious-2024-58134

# CPANSA-Mojolicious-2024-58135 / CVE-2024-58135
#   "`mojo generate app` writes a weak rand()-based secret into the generated
#   configuration."
#   Not applicable: GPForum is not generated by `mojo generate app` and does not
#   read a generated secret file. Secrets come from the operator environment
#   (/etc/gpforum/gpforum.env) and are never committed.
#   Upstream publishes no fixed version; the mitigation is configuration.
CPANSA-Mojolicious-2024-58135
