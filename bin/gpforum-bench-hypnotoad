#!/usr/bin/env perl
package main;

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use GPForum::Command::HypnotoadBenchmark;

our $VERSION = '0.001';

exit GPForum::Command::HypnotoadBenchmark->new->run(@ARGV);
