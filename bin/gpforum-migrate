#!/usr/bin/env perl
package main;

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use GPForum::Command::Migrate;

our $VERSION = '0.001';

exit GPForum::Command::Migrate->new->run(@ARGV);
