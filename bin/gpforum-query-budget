#!/usr/bin/env perl
package main;

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use GPForum::Command::QueryBudget;

our $VERSION = '0.001';

exit GPForum::Command::QueryBudget->new->run(@ARGV);
