# GPForum Product Flows

Date: 2026-05-24.

This document records commit 5 product-flow completion. The scope is deliberately
limited to workflows already present in the repository. No social-network layer,
plugin marketplace, external service, Redis dependency, OpenSearch dependency,
or SPA-first architecture is introduced here.

## Flow Status

| Flow | Status | Controller | Service boundary | Tests | Notes |
| --- | --- | --- | --- | --- | --- |
| Registration | complete | `Identity` | `Identity::Workflow`, `Identity::Registration`, `Identity::Store`, `Identity::RegistrationStore`, `Identity::Mailer`, `Worker::Handler::IdentityMail`, `CommandIdempotency` | `t/06-identity-web.t`, `t/07-registration-service.t`, `t/08-identity-store.t`, `t/103-identity-workflow.t`, `t/112-identity-registration-store.t`, `t/146-identity-mailer.t`, `t/147-identity-email-verification.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t`, `t/154-identity-mail.t` | Creates a pending user and password credential, emits event/audit, queues a verification message on the outbox, and returns non-enumerative duplicate errors. A unique race on username or email returns those same errors and does not insert a second pending user. A unique race on user `id` remints the id once and does not return another user's account. A unique race on an active password credential reuses the existing secret and does not insert a second active password. A unique race on leftover credential `id` with this user reuses the active password. A unique race on credential `id` remints the id once and does not return another user's credential. POST `/register` mints and requires `command_id`; a lost response retried with the same key replays from `command_log` and does not persist a second pending account. Store and command-log failures return HTTP 503 without leaking the exception. |
| Login/logout | complete | `Identity` | `Identity::Workflow`, `Identity::Store`, `Identity::AuthStore`, `Identity::AccountStore`, `Web::CookieSession`, `Password`, `SessionToken`, `Identity::SecurityAudit`, `CommandIdempotency` | `t/06-identity-web.t`, `t/08-identity-store.t`, `t/50-security-hardening.t`, `t/61-mvp-user-flow.t`, `t/70-bootstrap-identity.t`, `t/103-identity-workflow.t`, `t/110-identity-account-store.t`, `t/111-identity-auth-store.t`, `t/114-web-cookie-session.t`, `t/147-identity-email-verification.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Verifies Argon2id credentials, creates server-side session rows, rotates cookie-session identity through `Web::CookieSession`, revokes server-side sessions on logout, completes password/email commands through the account store, and audits login/logout requests. POST `/login` and POST `/logout` mint and require `command_id`; a lost login retried with the same key replays from `command_log` and does not open a second session; a lost logout retried with the same key does not revoke twice. A second store revoke of an already-revoked session keeps the original timestamp. The recorded login result is JSON-safe session identity. A unique race on `session_hash` remints the hash once and does not return another user's session. A unique race on leftover `session_id` with this user and hash reuses the session. A unique race on `session_id` remints the id once and does not return another user's session. Store and command-log failures return HTTP 503 without leaking the exception. Pending members do not receive a session until email verification; already-active bootstrap admins do not need `email_verified_at`. |
| Email lifecycle | complete | `Identity::Password`, `Identity::Email`, `Identity::Verification` | `Identity::Workflow`, `Identity::AccountStore`, `Identity::Mailer`, `Worker::Handler::IdentityMail`, `CommandIdempotency` | `t/06-identity-web.t`, `t/08-identity-store.t`, `t/16-workers-phase.t`, `t/80-settings-web.t`, `t/103-identity-workflow.t`, `t/110-identity-account-store.t`, `t/146-identity-mailer.t`, `t/147-identity-email-verification.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t`, `t/154-identity-mail.t` | Sends password-reset, email-change, and registration-verification mail from `GPFORUM_MAIL_*` (test transport in development, sendmail in staging/production, optional SMTP). Raw tokens are mailed then stripped from the workflow result and are never logged. Delivery is queued on the outbox in the same transaction as token issuance; EventLog does not store the raw token. Issuance, consume, and authenticated password-change POSTs mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not issue or consume a second token or rotate a password twice. A new `command_id` against an unused token rotates that row instead of inserting another. A unique race on `token_hash` remints the hash once and does not return another user's token. A unique race on leftover `token_id` with this user and hash reuses the token. A unique race on `token_id` remints the id once and does not return another user's token. A store retry of email-change request for the member's already-verified address does not issue another token. A store retry of email verification against an already-active verified user keeps the original `email_verified_at`. A store retry of email-change confirmation for the same already-verified address keeps the original timestamp. A unique race on `email_normalized` during confirm returns `email_already_registered` and does not restamp the user. A store retry of password change with the same secret does not rotate the credential. A store retry of password reset to the same secret still consumes the token and revokes sessions, but does not rotate the credential. Password-change hashes include `user_id` only. Store and command-log failures return HTTP 503 without leaking the exception. |
| Public profile | complete | `Identity` | `Identity::ProfileReader` | `t/06-identity-web.t`, `t/42-profile-reader.t` | Public-safe profile view hides private email and exposes contributor summary plus public discussions. |
| Member settings | complete | `Identity::Settings` | `Identity::Workflow`, `Identity::PreferenceStore`, `Notification::Workflow`, `Notification::PreferenceStore` | `t/66-locale-preference.t`, `t/79-theme-preference.t`, `t/80-settings-web.t`, `t/103-identity-workflow.t`, `t/107-notification-workflow.t`, `t/109-identity-preference-store.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Authenticated members persist locale, theme, and notification channels through dedicated write workflows; cookies and session rotation stay HTTP. Guest and signed-in HTML selector writes set a success flash and redirect to `return_to`. Signed-in POST `/locale` and POST `/theme` mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry of the same locale or theme does not restamp `updated_at`. Guest cookie writes omit `command_id`. POST `/settings` mints and requires `command_id` for notification channels; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry of the same channel values does not restamp `updated_at`. A unique race on `(user_id, channel)` reuses the existing row and does not restamp `updated_at` when the values match. Locale and theme on that POST mint their own keys. POST `/settings/password` mints and requires `command_id`; a lost response retried with the same key replays from `command_log` and does not rotate twice after `current_password` is already the new secret. |
| Categories | complete | `Forum` | `Forum::CategoryReader` | `t/31-forum-http-readers.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t` | Lists and finds non-deleted categories with bounded limits and disposable local cache support. |
| Thread listing/show | complete | `Forum` | `ThreadReader`, `ThreadDetailReader`, `PostReader`, `PageWindow` | `t/21-forum-pagination.t`, `t/31-forum-http-readers.t`, `t/32-forum-web.t` | Uses keyset pagination, explicit visibility/moderation filters, and post body separation. |
| Thread/reply creation | complete | `Forum` | `PostingWorkflow`, `ThreadComposer`, `ThreadStore`, `PostComposer`, `PostStore`, `CommandIdempotency` | `t/11-forum-thread.t`, `t/12-forum-post.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/87-command-idempotency.t`, `t/153-lost-response-retry.t` | Writes canonical thread/post/body/revision/counter/event/audit/outbox records through existing store boundaries; supplied idempotency keys replay completed retries and reject mismatched payloads. A lost HTTP response retried with the same `command_id` returns the original ids and does not insert a second row. A unique race on leftover command_log `command_id` with this idempotency key reuses the command and finishes it. A unique race on command_log `command_id` remints the id once and does not replay another command. A unique race on leftover thread `thread_id` with this category and slug reuses the thread and inserts the missing opening post. A unique race on leftover opening `post_id` with this thread reuses the post and inserts the missing body. A unique race on leftover opening `body_id` with this post reuses the body and inserts the missing revision. A unique race on leftover opening `revision_id` with this post reuses the revision and inserts the missing counter. A unique race on thread `thread_id` remints the id once and does not return another thread. A unique race on leftover reply `post_id` with this thread reuses the post and inserts the missing body. A unique race on leftover reply `body_id` with this post reuses the body and inserts the missing revision. A unique race on leftover reply `revision_id` with this post reuses the revision and inserts the missing shard. A unique race on reply `post_id` remints the id once and does not return another post. A unique race on reply `(thread_id, position)` retries allocation once. A unique race on counter shard `(thread_id, shard_id)` applies the delta to the winner and does not drop the increment. HTML writes redirect to the new thread or reply with a success flash; JSON still returns the write payload. |
| Author post edit | complete | `Forum::Write` | `PostingWorkflow`, `PostComposer`, `PostStore`, `PostReader`, `CommandIdempotency` | `t/12-forum-post.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors append a new body/revision on `POST /p/:post_id`; locked, hidden, deleted, and foreign posts are rejected. `post.updated` reindexes search, invalidates cache, and reprojects feed rows. A store retry of the same body hash does not insert another revision or emit another event. A unique race on leftover edit `body_id` with this post reuses the body and inserts the missing revision. A unique race on leftover edit `revision_id` with this post reuses the revision and applies missing pointers. A unique race on `body_id` or `revision_id` reuses the existing edit and does not write a second event when pointers already match. A unique race on `(post_id, revision_number)` retries allocation once. HTML writes redirect to the post with a success flash; JSON still returns the write payload. |
| Author post delete | complete | `Forum::Write` | `PostingWorkflow`, `PostStore`, `PostReader`, `CommandIdempotency` | `t/12-forum-post.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors soft-delete on `POST /p/:post_id/delete`; locked, hidden, already-deleted, and foreign posts are rejected. `post.deleted` removes search and feed rows and invalidates cache. Moderation hide stays on `post.hidden`. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Author post restore | complete | `Forum::Write` | `PostingWorkflow`, `PostStore`, `PostReader`, `CommandIdempotency` | `t/12-forum-post.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors restore on `POST /p/:post_id/restore`. The author still sees the deleted post on the thread. `post.undeleted` reindexes search, reprojects feed, and invalidates cache. Reputation ignores it so author delete/restore does not grant `post.restored` points. HTML writes redirect to the post with a success flash; JSON still returns the write payload. |
| Author thread title edit | complete | `Forum::Write` | `PostingWorkflow`, `ThreadComposer`, `ThreadStore`, `CommandIdempotency` | `t/11-forum-thread.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors rename on `POST /t/:thread_id/edit`; the slug follows the title. Locked, hidden, deleted, and foreign threads are rejected. `thread.updated` reindexes search and invalidates cache. A store retry of the same title and slug does not bump version or emit another event. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Author thread delete | complete | `Forum::Write` | `PostingWorkflow`, `ThreadStore`, `CommandIdempotency` | `t/11-forum-thread.t`, `t/19-search.t`, `t/24-advanced-community.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t`, `t/148-feed-projection-handler.t` | Authors soft-delete on `POST /t/:thread_id/delete`; locked, hidden, already-deleted, and foreign threads are rejected. `thread.deleted` removes the thread search document, post search documents in that thread, the thread feed item, and post feed rows. Posts are hidden by the thread 404. Moderation hide stays on `moderation_state`. HTML writes redirect to the category with a success flash; JSON still returns the write payload. |
| Author thread restore | complete | `Forum::Write` | `PostingWorkflow`, `ThreadStore`, `ThreadReader`, `ThreadDetailReader`, `CommandIdempotency` | `t/11-forum-thread.t`, `t/19-search.t`, `t/31-forum-http-readers.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors restore on `POST /t/:thread_id/restore`. The author still sees the deleted thread on the category listing and thread page. `thread.undeleted` reindexes the thread and its posts, reprojects feed, and invalidates cache. Reputation ignores it so author delete/restore does not grant `thread.restored` points. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Author thread move | complete | `Forum::Write` | `PostingWorkflow`, `ThreadComposer`, `ThreadStore`, `CommandIdempotency` | `t/11-forum-thread.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/72-forum-bootstrap-workflow.t` | Authors recategorize on `POST /t/:thread_id/move`; locked, hidden, deleted, foreign threads, and missing categories are rejected. `thread.moved` reindexes search and invalidates origin plus destination category cache tags. A store retry of the same category does not bump version or emit another event. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Read-state | complete | `Forum`, `Forum::Write` | `Forum::ReadWorkflow`, `Forum::ReadState`, `CommandIdempotency` | `t/41-thread-read-state.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Marks thread read state through CSRF/authenticated POST and returns keyset-safe reading summary. POST `/t/:thread_id/read` mints and requires `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry that does not advance the stored position keeps the original `last_read_at`. A unique race on `(user_id, thread_id)` reloads the winner and keeps that stamp when the stored position is already at least the requested one. Store and command-log failures return HTTP 503 without leaking the exception. HTML writes redirect to the thread with a success flash; JSON still returns the read-state payload. |
| Bookmark | complete | `Forum::Community` | `Community::Workflow`, `Community::BookmarkStore`, `CommandIdempotency` | `t/24-advanced-community.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/61-mvp-user-flow.t`, `t/156-community-workflow.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Supports save, restore, remove, status, and user listing. POST bookmark and bookmark-remove mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry against an already-deleted bookmark keeps the original `deleted_at`. A unique race on leftover `bookmark_id` with this user and target reuses the bookmark. A store retry against an already-active bookmark with the same note does not rewrite the row. Command hashes include actor, target, and note only. Store and command-log failures return HTTP 503 without leaking the exception. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Subscription | complete | `Forum::Community` | `Community::Workflow`, `Notification::SubscriptionStore`, `CommandIdempotency` | `t/17-notifications.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/61-mvp-user-flow.t`, `t/156-community-workflow.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Supports subscribe, mute, revoke, restore, status, and fanout recipient discovery. POST subscribe, mute, and unsubscribe mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry against an already-muted or already-revoked row keeps the original timestamp. A unique race on leftover `subscription_id` with this user and target reuses the subscription. A store retry against an already-active subscription with the same preference does not rewrite the row. Command hashes include actor, target, and preference only. Store and command-log failures return HTTP 503 without leaking the exception. HTML writes redirect to the thread with a success flash; JSON still returns the write payload. |
| Notifications | complete | `Notifications`, `Notifications::Read`, `Identity::Settings` | `Notification::Workflow`, `Notification::Dispatcher`, `Notification::PreferenceStore` | `t/17-notifications.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/50-security-hardening.t`, `t/80-settings-web.t`, `t/106-notification-controllers.t`, `t/107-notification-workflow.t`, `t/134-web-notification-access.t` | Lists inbox, marks one row or the whole inbox read, and persists channel preferences through a dedicated write workflow that mints and requires HTTP `command_id`. A lost `POST /settings` retried with the same key replays from `command_log` and does not persist twice. A store retry of the same channel values does not restamp `updated_at`. A unique race on `(user_id, channel)` reuses the existing row and does not restamp `updated_at` when the values match. Missing inbox rows still return coherent `404`. HTML mark-read and mark-all-read redirect to the inbox with a success flash; JSON still returns the write payload. Mark-all is idempotent when every row is already read. A unique race on inbox `(recipient_user_id, notification_id)` reuses the existing delivery and does not insert a second notification. A unique race on read `(notification_id, recipient_user_id)` reuses the existing row and does not insert a second read. |
| Mentions | complete | `Notifications::Mentions`, `Forum` | `MentionExtractor`, `MentionStore`, `MentionReader` | `t/24-advanced-community.t`, `t/32-forum-web.t`, `t/106-notification-controllers.t` | Extracts unique mentions, skips unknown/self mentions, creates mention notifications, and exposes recipient inbox. A unique race on leftover `mention_id` with this source and mentioned user reuses the mention and inserts the missing notification. A unique race on `(source_type, source_id, mentioned_user_id)` reuses the existing row and does not notify again. |
| Attachments | complete | `Attachments`, `Attachments::Upload` | `Attachment::Workflow`, `UploadPipeline`, `Delivery`, `Store`, `CommandIdempotency` | `t/22-attachments.t`, `t/63-attachments-web.t`, `t/104-attachment-controllers.t`, `t/105-attachment-workflow.t`, `t/123-attachment-lifecycle.t`, `t/133-web-attachment-access.t`, `t/35-forum-accessible-ssr.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Authors upload and link files to visible posts and soft-delete those links; downloads enforce delivery visibility and return `404`/`403` for missing or unavailable objects. POST upload and delete mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not store a second file or soft-delete twice. Command hashes include actor and target ids only, never the uploaded bytes. Store and command-log failures return HTTP 503 without leaking the exception. HTML upload and delete redirect to the post with a success flash; JSON still returns the write payload. Delete replays when the attachment is already deleted. A media retry against an existing thumbnail does not reread the original object or insert another variant. A unique race on an attachment intent `object_key` reuses the existing row and does not write a second event. A unique race on leftover `attachment_id` with this object key reuses the attachment and inserts the missing event. A unique race on `attachment_id` remints the id and object key once and does not return another attachment. A unique race on leftover `attachment_link_id` with this target reuses the link. A unique race on an attachment link or variant reuses the existing row. A unique race on `attachment_link_id` remints the id once and does not return another link. A unique race on leftover `attachment_variant_id` with this type and object key reuses the variant. A unique race on a variant `object_key` reuses the stored blob. A unique race on `attachment_variant_id` remints the id once and does not return another variant. An already-clean or already-infected scan does not reread the stored object; a failed scan still rereads. |
| Reports | complete | `Forum`, `Moderation` | `Community::Workflow`, `Moderation::ReportStore`, `CommandIdempotency` | `t/25-moderation-review.t`, `t/32-forum-web.t`, `t/35-forum-accessible-ssr.t`, `t/43-moderation-web.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t`, `t/156-community-workflow.t` | Authenticated users can report threads, posts, and profiles; member POSTs mint and require `command_id`. A lost report retried with the same key replays from `command_log` and does not insert a second row. Unique open/triaged reporter-target rows remain the store-level guard. A unique race on leftover `report_id` with this reporter and target reuses the report and inserts the missing event. Store and command-log failures return HTTP 503 without leaking the exception. Moderators assign, release, and resolve with a per-form `command_id`. HTML reports redirect to the thread or profile with a success flash; JSON still returns the report payload. |
| Moderation | complete | `Moderation`, `Moderation::Queue`, `Moderation::Actions`, `Moderation::Suspensions` | `Workflow`, `ActionStore`, `ReportStore`, `SuspensionStore`, `ReviewReader`, `CommandIdempotency` | `t/25-moderation-review.t`, `t/43-moderation-web.t`, `t/50-security-hardening.t`, `t/94-moderation-controllers.t`, `t/95-moderation-workflow.t`, `t/135-web-moderation-access.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Covers queues, action history, hide/restore for posts and threads, lock/unlock, reverse, suspend, and revoke through a dedicated write workflow. Assign/release/resolve/reverse mint and require `command_id` at HTTP; a lost response retried with the same key replays from `command_log` and does not persist twice. Hide/restore/lock/unlock mint and require `command_id` at HTTP; a lost response retried with the same key replays from `command_log` and does not persist twice. ActionStore unique `command_id` remains the store-level guard. A unique race on leftover `moderation_action_id` with this command_id reuses the action and inserts the missing event. A hide, restore, lock, or unlock whose target is already in the expected state returns the existing action without a second event, audit, or outbox row. Suspend and revoke mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry of an already-revoked suspension restores the user if still suspended and does not restamp an already-active user or emit another event. Thread hide removes search documents and feed rows for the thread and its posts. HTML writes redirect to the report queue with a success flash; JSON still returns the action payload. A lost hide HTTP response retried with the same `command_id` returns the original action and does not insert a second row. Store and command-log failures return HTTP 503 without leaking the exception. |
| Admin roles/permissions | complete | `Admin`, `Admin::Catalog`, `Admin::Bindings`, `Admin::Categories` | `Admin::Workflow`, `RoleCatalog`, `RoleBindingStore`, `CategoryStore`, `PermissionReview`, `PermissionGate`, `AuditReview`, `CommandIdempotency` | `t/26-admin-authorization.t`, `t/44-admin-web.t`, `t/50-security-hardening.t`, `t/96-admin-controllers.t`, `t/97-admin-workflow.t`, `t/137-web-admin-access.t`, `t/145-admin-category-store.t`, `t/152-write-unavailable.t`, `t/153-lost-response-retry.t` | Authorization distinguishes `401` anonymous from `403` permission denied and keeps writes CSRF-protected through a dedicated admin workflow. POST role, permission, attach, bind, revoke, and category writes mint and require `command_id`; a lost response retried with the same key replays from `command_log` and does not persist twice. A store retry of an unchanged category does not bump version or emit another event. A unique race on leftover `role_id` with this name reuses the role and inserts the missing audit. A unique race on leftover `permission_id` with this resource and action reuses the permission and inserts the missing audit. A unique race on leftover role-permission with this role and permission reuses the grant and inserts the missing audit. A unique race on leftover `binding_id` with this active binding reuses the binding and inserts the missing audit. A unique race on leftover `space_id` with the default `general` slug reuses the space. A catalog unique race on role, permission, or attach reuses the existing row and does not write a second audit. A unique race on leftover category `category_id` with this space and slug reuses the category and inserts the missing event. A unique race on category `(space_id, slug)` reuses the existing row and does not write a second event. A unique race on the default `general` space slug reuses the existing space. A unique race on an active role binding reuses the existing row and does not write a second audit. Store and command-log failures return HTTP 503 without leaking the exception. HTML writes redirect to the roles or categories catalog with a success flash; JSON still returns the write payload. |
| Privacy rights | complete | `Privacy`, `Privacy::Requests`, `Privacy::Review` | `Workflow`, `DeletionWorkflow`, `ExportBundleBuilder`, `RetentionHoldStore`, `DataRightsReview`, `CommandIdempotency` | `t/27-import-export.t`, `t/29-privacy-rights.t`, `t/62-privacy-web.t`, `t/86-engineering-correctness.t`, `t/100-privacy-controllers.t`, `t/101-privacy-workflow.t`, `t/125-privacy-completion.t`, `t/136-web-privacy-access.t`, `t/153-lost-response-retry.t` | Members request export and deletion; staff approve, hold, or erase through a dedicated privacy workflow. Writes mint and require `command_id`. Open deletion requests, pending exports, and active holds replay on retry and are unique in schema. Import failure rows reuse unique `(import_job_id, source_record_type, source_record_id)` on retry. A unique race on leftover `legacy_id_map_id` with this source reuses the mapping. A unique race on leftover `deletion_request_id` with this open resource reuses the request and inserts the missing event. A unique race on leftover `retention_hold_id` with this resource reuses the hold and inserts the missing event. A unique race on leftover `export_request_id` with this pending unique reuses the request and inserts the missing event. A unique race on leftover `import_failure_id` with this source reuses the failure. A unique race on leftover `erasure_job_id` with this request reuses the job and inserts the missing approval action. The same export `command_id` replays from `command_log` after complete. A lost export HTTP response retried with the same `command_id` returns the original request and does not create another bundle. An erasure job blocked by a legal hold replays the block until the hold ends. An incomplete block retry restores `held` status and `last_error` without a second event, and does not restamp those columns when they already match. An already-done erasure retry completes the deletion request if it is still open, keeps the original `completed_at`, and does not emit a second action. A concurrent approval whose job lookup misses reuses the unique erasure job and does not insert a second action. Active legal holds return `409`. A timeout after credential and session revocation rolls back the user identity; a later `complete_job` finishes erasure. HTML writes redirect to the member dashboard or staff review with a success flash; JSON still returns the write payload. Completing an export stores the member's posts, attachments, inbox rows, subscriptions, preferences, and profile (including email) on the request manifest; EventLog keeps counts only. `GET /privacy/export/:export_request_id` downloads that JSON for the subject. |
| Search | complete | `Forum` | `Search::Searcher` | `t/19-search.t`, `t/32-forum-web.t` | PostgreSQL-native derived search and autocomplete remain visibility/permission-aware and bounded. `thread.deleted` and `thread.hidden` remove the thread document and every post document in that thread. `thread.restored` and `thread.undeleted` reindex the thread and those posts. A store retry of an unchanged document keeps the original `indexed_at`. A unique race on `(entity_type, entity_id)` reuses the existing document and does not restamp `indexed_at` when the copy is unchanged. A unique race on projection generation `(projection_name, built_from_event_id)` reuses the rebuild. A unique race on projection offset `projection_name` reuses the existing row and does not restamp `updated_at` when `last_event_id` matches. |
| Public feed | complete | `Discovery` | `FeedBuilder`, `ThreadReader`, `VisibilityPolicy` | `t/30-public-discovery.t`, `t/32-forum-web.t` | Atom feed exposes only public visible excerpts, not full private/moderated content. |
| Sitemap/robots/canonical metadata | complete | `Discovery`, `Forum` | `SitemapBuilder`, `RobotsPolicy`, `CanonicalUrl`, `MetadataBuilder` | `t/30-public-discovery.t`, `t/32-forum-web.t` | Public discovery is canonical, noindex-safe for restricted content, and excludes hidden/deleted resources. The sitemap also lists `/legal/terms`, `/legal/privacy`, and `/legal/cookies`. |
| Legal pages | complete | `Legal` | `Web::LegalAccess`, `CanonicalUrl`, `SitemapBuilder` | `t/03-home.t`, `t/30-public-discovery.t`, `t/32-forum-web.t`, `t/67-ui-system.t`, `t/73-bootstrap-composition.t`, `t/78-web-operations-discovery-payloads.t`, `t/155-legal-pages.t` | Anonymous GET pages for terms, privacy notice, and cookies. Footer links them. JSON returns catalog keys. Italian locale is covered. Operators still replace the catalog copy with counsel-reviewed instance policy. |
| Realtime | complete | `Realtime` | `Web::RealtimeAccess`, `Realtime::Hub`, `ConnectionRegistry`, `ChannelAuthorizer`, `PgNotifier`, `PgListener`, `ListenerSupervisor` | `t/20-realtime.t`, `t/81-realtime-operational.t`, `t/82-realtime-supervisor.t`, `t/113-web-realtime-access.t` | Realtime is strict-auth, LISTEN/NOTIFY capable, supervised per web process, and remains an enhancement over polling. Handshake origin and payload decisions live outside the controller. |
| Personal feed | complete | `Forum` | `FeedReader`, `FeedProjector` via outbox `FeedProjection` | `t/24-advanced-community.t`, `t/148-feed-projection-handler.t`, `t/16-workers-phase.t` | Authenticated `/feed` reads `user_feed_items`. Created and restored posts/threads project rows for the author and active thread subscribers; author `post.undeleted` and `thread.undeleted` reproject those rows; `post.hidden` and author `post.deleted` delete those rows; author `thread.deleted` and moderation `thread.hidden` delete the thread item and every post item in that thread. A store retry of the same created_at, rank, and version columns does not rewrite the row. A unique race on `(user_id, item_type, item_id)` reloads the winner and keeps that copy when those columns match. |
| Worker delivery | complete | workers | `Outbox::Dispatcher`, `DomainEventTransport`, `MinionGuard`, `MinionRegistrar`, `HandlerIdempotency`, `EventIdempotencyStore`, worker handlers | `t/13-outbox-dispatcher.t`, `t/16-workers-phase.t`, `t/83-outbox-worker-wiring.t`, `t/84-outbox-concurrent-dispatcher.t`, `t/121-outbox-boundaries.t`, `t/148-feed-projection-handler.t`, `t/149-reputation-update-handler.t`, `t/150-outbox-handler-idempotency.t`, `t/154-identity-mail.t` | Outbox dispatch is wired to real handlers through a direct worker command and optional Minion registration; feed projection, reputation updates, and identity mail run on the same transport as search, notifications, and cache. Reputation events require `source_id`; a missing source is skipped without a delta, and `(user_id, source_type, source_id)` is unique for every row. A unique race on leftover `reputation_event_id` with this source reuses the event and inserts the missing snapshot. A unique race on `reputation_event_id` remints the id once and does not return another event. A new event that does not change trust level still writes the snapshot and event, but does not restamp the user row. A unique race on trust snapshot `user_id` applies this event's delta to the winner. Handler and realtime replay uses `worker.<name>:{event_id}` keys inserted only after successful handle, so a crash before outbox ack retries without duplicating skip-wrapped side effects. Identity mail is not skip-wrapped: a crash after send and before outbox ack resends from the outbox payload because EventLog omits the raw token. A crash after claim and before dispatch leaves the row `running` under an exclusive lock; after the lock expires another worker delivers once. A classified `permanent` failure cancels and dead-letters on that attempt; cancelled rows are not claimed again. A unique race on leftover `dead_letter_id` with this source reuses the review row. A unique race on dead-letter `(source_table, source_id)` reuses the review row. A unique race on leftover `outbox_id` with this handoff key reuses the outbox row. A unique race on leftover `event_id` with this idempotency key reuses the event and inserts the missing outbox. A unique race on outbox `idempotency_key` reuses the EventLog row and does not insert a second outbox message. `GPFORUM_MINION_ENABLED=1` fails closed when the Minion PostgreSQL backend is absent; `bin/gpforum-outbox-dispatch` skips Minion. |

## Completion Rules Applied

* Controllers remain thin and delegate workflow work to service boundaries.
* CSRF is enforced on every current POST route.
* Anonymous protected reads/writes return `401`.
* Authenticated users without permission return `403`.
* Missing resources return `404` where the workflow can distinguish absence.
* User-facing identity failures avoid account enumeration.
* Search, feed, sitemap, metadata, notifications, and autocomplete remain
  permission/moderation safe.
* Pagination remains keyset-based and now ignores malformed cursors rather than
  letting them become request-path failures.

## Residual Risk Register

| Area | Risk | Current mitigation | Priority |
| --- | --- | --- | --- |
| Email lifecycle | Mail send is outbox-queued in the token transaction; a delivery failure retries without rolling back issuance. | `IdentityMail` handler plus `Identity::Mailer`. Raw tokens are not logged or written to EventLog. Completed outbox rows are purged. | low |
| Multi-process rate limiting | PostgreSQL-backed limiter exists; local memory remains degraded fallback. | `/metrics` exposes primary failures, fallback usage and blocked decisions. | low |
| Realtime fanout | Websocket state is process-local. | Each web process can run a supervised LISTEN/NOTIFY listener; polling remains canonical fallback. | low |
| Worker operations | Minion backend is opt-in and requires an explicit PostgreSQL URL plus a reachable backend. | Direct outbox dispatch is always available and skips Minion; web/Minion startup fails closed if Minion is enabled and the backend is absent. | low |
| Rich content rendering | Safe rendered body boundary is trusted by templates. | Tests preserve escaping/sanitized-body contract. | high before rich renderer |

## Commands

```sh
carton exec prove -lr t/06-identity-web.t t/07-registration-service.t t/08-identity-store.t
carton exec prove -lr t/17-notifications.t t/21-forum-pagination.t t/31-forum-http-readers.t t/32-forum-web.t
carton exec prove -lr t
script/perltidy-check
script/perlcritic --severity 5
script/architecture-check
script/query-plan-check
```

Result:

* product-flow test set passed: 7 files, 390 tests;
* full test suite passed: 55 files, 2767 tests;
* `script/perltidy-check` passed;
* `script/perlcritic --severity 5` passed;
* `script/architecture-check` passed;
* `script/query-plan-check` passed with 23 indexed query plans, 0 `OFFSET`
  violations, and DB-backed evidence activation when a DSN is configured.

`script/query-budget --check` passes when pointed at the synchronized local
Postgres.app evidence database. `--sync` does not rewrite an already-aligned
catalog row.

## Next Product Priorities

1. Run PostgreSQL-backed rate-limit evidence against a seeded database.
2. Add fixture-backed browser walkthrough for register -> login -> thread ->
   reply -> notification using a real PostgreSQL test database.
